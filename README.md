# Doomemacs config

Delete old files and clone this repo inside you doom config directory `~/.config/doom`.

As there is copilot mentionned inside the config, you will need on the first start of emacs to install it through `M-x` as usual.
